---
import '../styles/global.css';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';

interface Props {
  title: string;
  description: string;
  /**
   * True (the default) for pages that open with a dark photographic hero: the
   * header overlays it. False for pages with no hero, which get a solid black
   * sticky header instead — the white wordmark needs a dark background either
   * way.
   */
  darkHeader?: boolean;
}

const { title, description, darkHeader = true } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="preload" href="/fonts/archivo-variable.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/inter-variable.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site)} />
  </head>
  <body>
    <a href="#main" class="skip-link sr-only focus:not-sr-only">Skip to content</a>
    <Header mode={darkHeader ? 'transparent' : 'solid'} />
    <slot />
    <Footer />
  </body>
</html>

<style>
  /* The header overlays the top of the page, so the skip link has to lift out
     of flow when focused or it appears underneath it. */
  .skip-link:focus {
    position: fixed;
    top: 8px;
    left: 8px;
    z-index: 100;
    padding: 12px 18px;
    background: var(--color-red);
    color: #fff;
    font-family: var(--font-display);
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.04em;
  }
</style>
